# GPU override (NVIDIA Container Toolkit required). Usage:
#   docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d --build

services:
  minatalker:
    gpus: all
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
